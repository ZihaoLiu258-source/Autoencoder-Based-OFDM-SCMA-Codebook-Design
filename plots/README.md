# Plotting

The MATLAB scripts in `matlab/` read sweep data from `artifacts/main/sweeps/` and write publication figures to `artifacts/main/figures/`. Paths are resolved from each script's location, so MATLAB's current directory does not need to be the repository root.

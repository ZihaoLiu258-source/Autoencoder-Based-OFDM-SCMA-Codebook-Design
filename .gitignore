# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/

# IDE / editor / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs and scratch
*.log
!artifacts/m1_randomized_training/*.log
!artifacts/m3_no_med/*.log
!artifacts/m4_seed_check/*.log
!artifacts/m4_seed_check/*/*.log
scratch/
tmp/
*.tmp

# MATLAB autosave / build artifacts
*.asv
*.m~
*.fig
octave-workspace
slprj/

# Generated figures (regenerated by the plot_*.m scripts).
# Keep a curated figure for the README under docs/ and force-add it if needed.
BER_vs_*.pdf
BER_vs_*.eps
BER_vs_*.png

# Local-only settings
*.local.*

# Uncomment if training artifacts get large.
# The codebooks in this repo are small (< 1 MB), so we keep them tracked.
# *.pt
# *.npy
